"""ROS graph discovery (docs/02-ros2-architecture.md). Owns the mechanical walk of the
live ROS 2 graph; assigns no meaning to what it finds (that is
ros_mcp.capabilities.CapabilityInferenceEngine's job)."""
