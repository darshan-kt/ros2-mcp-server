"""Capability inference (docs/03-capability-discovery.md): maps a raw ROS graph
snapshot onto the small, fixed semantic ontology, with confidence + config resolution.
Assigns no ROS-mechanical meaning itself — that is ros_mcp.discovery's job."""
